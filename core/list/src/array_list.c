#include "array_list.h"
#include <stdlib.h>

ArrayList ArrayListNew(int capacity){
    size_t[] array = {};
    ArrayList lit ={
        .size =0,
        .capacity=capacity,
        .idx=-1,
        .items = size_t[capacity]
    };
}